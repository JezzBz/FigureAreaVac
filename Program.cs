using FigureArea.Interfaces;
using FigureArea.Models;
using System;
using System.Collections.Generic;

namespace FigureArea
{
    class Program
    {
        static void Main(string[] args)
        {
/*
            Figure testfigure = new Figure(); 

            Triangle triangle = new Triangle(new int[]{1,8,8});
            Circle circle = new Circle(12);
            
            List<IFigure> array = new List<IFigure>{triangle,circle};
            foreach (var figure in array)
            {
                Console.WriteLine(testfigure.GetArea(figure));
            }*/
        }
    }
}
